# The workload itself installs nothing, so there is nothing to remove here.
# Uninstalling the workload lets the package manager remove the dependency
# packages according to its own rules.
Write-Host 'ADS Gamepad engineering workload removed.'
