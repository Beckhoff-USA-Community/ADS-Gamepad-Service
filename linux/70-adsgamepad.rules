# Grants the gamepad group access to a wired PlayStation 5 DualSense
# controller, so the ADS Gamepad Service can read it without running as root.
KERNEL=="hidraw*", ATTRS{idVendor}=="054c", ATTRS{idProduct}=="0ce6", GROUP="gamepad", MODE="0660"
